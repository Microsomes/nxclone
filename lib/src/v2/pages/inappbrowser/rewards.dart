import 'package:flutter/material.dart';

class RewardsInBrowser extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),

      body: Center(child: Text("inapp"),),

    );
  }

}